# Qwen3-14B 测试脚本 - 新文件说明

## 创建的文件总览

我为您创建了以下文件来解决 Qwen3-14B 权重加载问题：

```
ShieldLM/
├── test_qwen3_direct.py                # 主测试脚本（更新）⭐⭐
├── verify_qwen3_support.py             # Qwen3 支持验证脚本（新）
│
├── README_qwen3_direct.md              # 详细使用文档（新）
├── QUICKSTART_qwen3_direct.md          # 快速开始指南（更新）
│
├── TEST_QWEN3_README.md                # 问题汇总（整合后）⭐⭐
├── WEIGHT_CONVERSION_ISSUE.md          # 问题1：权重转换分析
├── CONFIG_MISMATCH_SOLUTION.md         # 问题2：配置不匹配⭐
├── NUMPY_TO_PARAMETER_FIX.md           # 问题3：numpy转换⭐⭐
├── MEMORY_ISSUE_SOLUTION.md            # 问题4：内存分配失败（新）⭐⭐⭐
│
├── 常见问题快速解决.md                 # 所有问题汇总（更新）⭐⭐⭐
├── 完整问题解决路径.md                 # 问题演进历史（新）⭐⭐
│
├── 权重转换问题-快速解决.md            # 问题1快速指南（新）
├── 配置不匹配-快速解决.txt             # 问题2快速指南（新）
│
├── 问题解决总结.txt                    # 第一次总结（新）
└── 新文件说明.md                       # 本文件（更新）
```

## 文件说明

### 1. `test_qwen3_direct.py` - 主测试脚本

**用途**：直接使用 mindformers 的 Qwen3 模块加载模型和权重，测试 generate 和 infer 功能。

**核心特性**：
- ✅ 直接导入 `mindformers.models.qwen3`，无需额外配置
- ✅ 自动检测和加载 safetensors/ckpt 格式权重
- ✅ 自动转换 HuggingFace 权重名称到 MindFormers 格式
- ✅ 详细的权重加载验证
- ✅ 支持 Legacy 和 MCore 两种模型架构
- ✅ 完整的错误处理和诊断信息

**与原脚本的主要区别**：

| 特性 | test_qwen3_direct.py | test_qwen3_mindformers.py |
|------|---------------------|--------------------------|
| 导入方式 | 直接导入 Qwen3ForCausalLM | 使用 AutoModel |
| 权重加载 | 显式加载 + 自动转换 | 依赖 AutoModel 自动加载 |
| 调试信息 | 非常详细 | 一般 |
| 适用场景 | 调试权重加载问题 | 常规使用 |

**使用示例**：
```bash
python ShieldLM/test_qwen3_direct.py \
    --config /path/to/predict_qwen3_14b.yaml \
    --model_dir /path/to/Qwen3-14B-weights \
    --test_mode both
```

---

### 2. `verify_qwen3_support.py` - 验证脚本

**用途**：快速验证您的 mindformers 安装是否包含 Qwen3 支持。

**检查项目**：
1. ✓ mindformers 是否已安装
2. ✓ qwen3 模块是否存在
3. ✓ Qwen3 相关类是否可导入
4. ✓ 模型是否已注册到 MindFormer
5. ✓ 能否创建 Qwen3Config
6. ✓ 能否创建 Qwen3ForCausalLM

**使用示例**：
```bash
python ShieldLM/verify_qwen3_support.py
```

**预期输出（成功）**：
```
============================================================
✓ Qwen3 支持验证通过！
============================================================

您可以直接使用以下方式导入 Qwen3:
  from mindformers.models.qwen3 import Qwen3Config, Qwen3ForCausalLM
```

**建议**：在使用主测试脚本之前，先运行此脚本确认环境配置正确。

---

### 3. `README_qwen3_direct.md` - 详细文档

**用途**：完整的使用说明和问题排查指南。

**包含内容**：
- 📖 详细的参数说明
- 📖 多个使用示例
- 📖 权重格式说明
- 📖 权重名称映射表
- 📖 常见问题及解决方案
- 📖 环境变量说明
- 📖 输出示例

**适用场景**：
- 需要了解所有参数的详细含义
- 遇到问题需要排查
- 需要自定义配置

---

### 4. `QUICKSTART_qwen3_direct.md` - 快速开始

**用途**：帮助您在 5 分钟内快速上手。

**包含内容**：
- 🚀 三步使用流程
- 🚀 核心代码示例
- 🚀 常见问题快速解答
- 🚀 性能优化提示

**适用场景**：
- 第一次使用
- 需要快速了解基本用法
- 需要代码示例

---

### 5. `WEIGHT_CONVERSION_ISSUE.md` - 权重转换问题分析 ⭐

**用途**：深入分析 `InferenceQwen3ForCausalLM does not implemented convert_weight_dict` 错误及解决方案。

**包含内容**：
- 🔍 问题根本原因分析（源码级别）
- 🔍 模型架构对比（Inference vs Training）
- 🔍 三种解决方案详解
- 🔍 权重转换技术细节
- 🔍 HuggingFace → MindFormers 映射规则
- 🔍 QKV/FFN 权重合并原理

**重要发现**：
- ❌ `InferenceQwen3ForCausalLM` 缺少 `convert_weight_dict` 方法
- ✅ `TrainingQwen3ForCausalLM` 有完整的转换实现
- 💡 解决方案：使用 `--use_training_conversion` 参数

**适用场景**：
- 遇到权重转换错误
- 想了解为什么会出现此问题
- 需要技术细节说明

---

### 6. `CONFIG_MISMATCH_SOLUTION.md` - 配置不匹配问题解决 ⭐⭐

**用途**：解决模型配置与权重文件不匹配的问题（如 hidden_size 错误）。

**包含内容**：
- 🔧 配置不匹配的原因分析
- 🔧 两种解决方案（自动/手动）
- 🔧 常见 Qwen3 模型的正确配置
- 🔧 脚本的自动检测和修复功能
- 🔧 完整的诊断命令
- 🔧 手动创建 config.json 的方法

**重要发现**：
- ❌ YAML 配置的 `hidden_size=4096` 与权重的 `hidden_size=5120` 不匹配
- ✅ 权重目录的 `config.json` 包含正确配置
- 💡 脚本会自动从 `config.json` 加载配置（如果存在）
- 💡 如果配置不匹配，脚本会自动修复并重新创建模型

**适用场景**：
- ❗ 遇到 shape mismatch 错误
- ❗ hidden_size 或其他配置参数错误
- 需要了解正确的模型配置
- 需要诊断配置问题

---

## 推荐使用流程

### 首次使用

```bash
# 1. 验证 Qwen3 支持
python ShieldLM/verify_qwen3_support.py

# 2. 如果验证通过，运行测试（HuggingFace 格式权重）
python ShieldLM/test_qwen3_direct.py \
    --config your_config.yaml \
    --model_dir your_model_directory \
    --use_training_conversion  # ⭐ 重要：用于 HuggingFace 格式权重

# 3. 查看输出，确认权重加载是否成功
```

**重要说明**：
- 如果权重是 **HuggingFace 格式** (safetensors)，**必须**使用 `--use_training_conversion`
- 这个参数解决了 `InferenceQwen3ForCausalLM` 缺少 `convert_weight_dict` 方法的问题
- 详细原因请查看 `WEIGHT_CONVERSION_ISSUE.md`

### 遇到问题时

#### 问题 1: `InferenceQwen3ForCausalLM does not implemented convert_weight_dict`

**快速解决**：
```bash
python ShieldLM/test_qwen3_direct.py \
    --config your_config.yaml \
    --model_dir your_weights \
    --use_training_conversion  # ⭐ 添加这个参数
```

**详细说明**：查看 `WEIGHT_CONVERSION_ISSUE.md`

---

#### 问题 2: 配置与权重不匹配（hidden_size 错误）⭐

**错误示例**：
```
RuntimeError: embedding.word_embeddings.weight shape mismatch
Got shape (151936, 4096) in net and shape (151936, 5120) in parameter_dict
```

**快速解决**：
1. **最简单**：确保权重目录有 `config.json` 文件，脚本会自动使用它
2. **手动修改**：修改 YAML 配置，将 `hidden_size` 改为 5120（Qwen3-14B）

```bash
# 只要权重目录有 config.json，脚本会自动修复
python ShieldLM/test_qwen3_direct.py \
    --config your_config.yaml \
    --model_dir /path/to/Qwen3-14B \  # 确保这里有 config.json
    --use_training_conversion
```

**详细说明**：查看 `CONFIG_MISMATCH_SOLUTION.md`

---

#### 其他问题

1. **先看快速开始**：`QUICKSTART_qwen3_direct.md`
2. **查看详细文档**：`README_qwen3_direct.md`
3. **检查环境**：重新运行 `verify_qwen3_support.py`
4. **查看详细日志**：注意脚本输出中的 ⚠️ 和 ❌ 标记

---

## 核心观点总结

根据您的观察，我们得出以下结论：

> **mindformers 已经包含 Qwen3 支持，只是缺少官方文档！**

这意味着：
1. ✅ 无需修改 mindformers 源码
2. ✅ 无需额外安装依赖
3. ✅ 直接导入即可使用：`from mindformers.models.qwen3 import Qwen3ForCausalLM`

新脚本的优势：
1. **简化导入**：不再尝试添加本地路径，直接使用已安装的 mindformers
2. **自动转换**：自动处理 HuggingFace 格式权重的名称转换
3. **详细诊断**：提供完整的权重加载验证和错误诊断
4. **灵活支持**：同时支持 safetensors 和 ckpt 格式

---

## 下一步建议

1. **运行验证脚本**：
   ```bash
   python ShieldLM/verify_qwen3_support.py
   ```

2. **如果验证通过**，运行测试脚本：
   ```bash
   python ShieldLM/test_qwen3_direct.py \
       --config your_config.yaml \
       --model_dir your_model_directory
   ```

3. **根据输出结果**：
   - ✓ 如果权重加载成功：恭喜！可以开始使用了
   - ⚠️ 如果有警告：查看详细日志，可能是部分参数未加载
   - ❌ 如果失败：查看 README_qwen3_direct.md 的"常见问题排查"部分

---

## 与原脚本的关系

- **test_qwen3_mindformers.py**（原始）：
  - 使用 AutoModel 自动加载
  - 适合常规使用场景
  - 依赖配置文件正确设置

- **test_qwen3_direct.py**（新）：
  - 直接使用 Qwen3 类
  - 适合调试和权重加载问题
  - 更详细的诊断信息
  - 自动处理权重转换

**建议**：
1. 先用 `test_qwen3_direct.py` 确认权重可以正确加载
2. 然后根据需要更新配置文件，使用 `test_qwen3_mindformers.py`

---

## 技术细节

### 权重名称映射

HuggingFace → MindFormers 转换规则（在 `utils.py` 中定义）：

```python
weight_mapping = [
    ('model.embed_tokens.', 'embedding.word_embeddings.'),
    ('.self_attn.q_proj.', '.self_attention.linear_q.'),
    ('.self_attn.k_proj.', '.self_attention.linear_k.'),
    ('.self_attn.v_proj.', '.self_attention.linear_v.'),
    ('.self_attn.o_proj.', '.self_attention.linear_proj.'),
    ('.mlp.gate_proj.', '.mlp.gating.'),
    ('.mlp.down_proj.', '.mlp.linear_fc2.'),
    ('.mlp.up_proj.', '.mlp.hidden.'),
    ('model.norm.', 'decoder.final_layernorm.'),
    ('lm_head.', 'output_layer.'),
    ('model.layers.', 'decoder.layers.')
]
```

### 模型选择逻辑

```python
# Qwen3ForCausalLM 会根据 RUN_MODE 选择实现
if os.environ.get("RUN_MODE") == "predict":
    return InferenceQwen3ForCausalLM(config=config)  # 推理优化
else:
    return TrainingQwen3ForCausalLM(config=config)   # 训练模式
```

---

## 反馈与改进

如果使用过程中遇到问题或有改进建议，可以：
1. 查看详细日志输出
2. 检查权重文件是否完整
3. 确认配置文件参数是否正确
4. 验证 mindformers 版本是否支持 Qwen3

祝您使用顺利！🎉

